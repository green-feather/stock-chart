CREATE TABLE dayData (
  id varchar(10),
  day smallint,
  stockPrice numeric(5,2)
);

CREATE TABLE dayData (
  id varchar(10),
  week smallint,
  stockPrice numeric(5,2)
);

CREATE TABLE dayData (
  id varchar(10),
  month smallint,
  stockPrice numeric(5,2)
);

CREATE TABLE dayData (
  id varchar(10),
  threeMonth smallint,
  stockPrice numeric(5,2)
);

CREATE TABLE dayData (
  id varchar(10),
  year smallint,
  stockPrice numeric(5,2)
);

CREATE TABLE dayData (
  id varchar(10),
  fiveYear smallint,
  stockPrice numeric(5,2)
);
